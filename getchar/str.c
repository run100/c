#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	/*
	char *p;
	scanf("%s", p);
	printf("%s\n", p);
	*/
    /*char* p;
	p =(char *)malloc(sizeof(char));　　 // 分配内存
	scanf("%s", p);
	printf("\n %s\n", p);
	free(p);
	*/
    
    char ch1[10],ch2[10];
	scanf("%s", ch1);
	gets(ch2);
    return 0;
}
